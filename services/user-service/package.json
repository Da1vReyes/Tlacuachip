{
  "name": "tlacuachic-user-service",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Owns users, business profiles, reports, progress, preferences, provider profiles and saved teams. Real Postgres persistence, JWT auth — every /api/me/* route acts on the caller's own account only, never on a client-supplied id.",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/index.js",
    "start": "node --env-file-if-exists=.env src/index.js",
    "seed:demo": "node --env-file-if-exists=.env src/seed-demo.js",
    "seed:test-network": "node --env-file-if-exists=.env src/seed-test-network.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.23.0"
  }
}
