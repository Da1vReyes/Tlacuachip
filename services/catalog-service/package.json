{
  "name": "tlacuachic-catalog-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Owns roadmap steps, mentors, and providers — public catalog data in the same Postgres database as user-service, seeded on first boot. Exposes an internal-key-protected route for user-service to sync real provider signups into the public roster.",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/index.js",
    "start": "node --env-file-if-exists=.env src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pg": "^8.23.0"
  }
}
